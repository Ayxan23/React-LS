import React, { useContext } from "react";
import MainContext from "../context/MainContext";

const Increase = () => {
    /*const context = useContext(MainContext)
    <div>
      <button onClick={()=>{
        context.setCounter(per => per += 1)
      }}>+</button>
    </div>*/
  return (
    <div></div>
  );
};

export default Increase;
